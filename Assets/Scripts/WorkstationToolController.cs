using UnityEngine;

namespace Com.ZiomtechStudios.ForgeExchange{
    public class WorkstationToolController : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}
